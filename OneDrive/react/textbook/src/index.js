import React from "react";
import ReactDOM from "react-dom/client";
import PassingCount from"./chapter_12/PassingCount.jsx";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
<div> <PassingCount /></div>
);